#!/usr/bin/env ruby
require "bundler/setup"
Bundler.require(:default)
require_relative "../app/spotify_extractor"

SpotifyExtractor.new(client_id: ARGV[1], client_secret: ARGV[2]).extract(user_id: ARGV[0])
